# dxd-chargen v138

## Visual Armor Coverage layout

- Moves **Visual Armor Coverage** into its own panel immediately before **Personal Armor** on Customize Armor.
- Removes the wide explanatory sidebar that constrained the silhouette in v137.
- Lets the SVG use the available center-column width, centered within a dedicated visualization area.
- Replaces the sidebar prose with a compact horizontal AR color legend beneath the silhouette.
- Keeps only a concise conditional note when one-side sectional components still require Left/Right assignment.

## Rules/data scope

No Armor rule, effective AR calculation, coverage atom, silhouette-selection rule, normalized runtime catalogue, CharacterDraft schema, character data, Library/Admin behavior, or CRS projection is changed in v138.
