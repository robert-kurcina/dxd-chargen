# Character Library Metadata v133

- Added explicit `utilities.libraryTags` arrays to all 23 active `character.json` records.
- Existing tags are normalized as unique trimmed strings; the current companion archive begins with no assigned administrator tags.
- Alba retains `trade-wizard` / `specialization-wizard-witch`.
- Twinkles retains `trade-cleric` / `specialization-cleric-cloister`.
- No source sheet, source character, portrait, or source image was modified.
- Armor and starting-gear normalization from v132 is retained unchanged.
