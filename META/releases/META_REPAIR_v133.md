# Character META Repair v133

Metadata-only archive repair. No `characters/` content was intentionally changed.

The v131 Armor normalization, v132 runtime normalization, and v133 Library metadata records were moved from the flat `META/` root into `META/releases/` so the companion archive follows the same release-record organization as dxd-chargen.
